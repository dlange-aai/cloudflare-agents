# AssemblyAI API key — required for streaming STT.
# Get one at https://www.assemblyai.com/app/api-keys
# Copy this file to `.dev.vars` and fill in your key before running `npm start`.
ASSEMBLYAI_API_KEY=
